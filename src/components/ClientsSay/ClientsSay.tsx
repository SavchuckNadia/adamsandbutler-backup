import styles from "./ClientsSay.module.scss"

const ClientsSay = () => {
  return (
    <section className={styles.clients_say}>
        <h2 className="secondary-heading">What our clients say</h2>
    </section>
  )
}

export default ClientsSay